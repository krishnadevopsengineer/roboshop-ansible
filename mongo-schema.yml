- name: Install MySQL Client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: Load Schema
  ansible.builtin.shell: mongo --host mongodb-dev.kdevops72.online </app/schema/{{component}}.js